# 本地开发变量示例：复制为 .dev.vars 后填写真实值。

# 首次开通 admin 用户时使用的引导密码：至少 8 位，生产环境建议至少 16 位
# 首次登录成功后请立即在后台“账号安全”中修改密码。
ADMIN_PASSWORD="请设置至少 12 位的强密码"

# 会话签名密钥：建议 32 字节以上的随机值，不设置时回退使用 ADMIN_PASSWORD 签名
# 生成：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="请设置至少 32 字节的随机字符串"