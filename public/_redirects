# Cloudflare Pages 重定向规则
# 参考：https://developers.cloudflare.com/pages/configuration/redirects/

# 让所有 SPA 前端路由都回退到 index.html（用于 /about, /contact 等）
# 但 /api/*, /admin/*, /r2/* 不 fallback（由后端处理）
/about    /index.html 200
/contact  /index.html 200

# 注意：不要为 /admin、/admin/login 写 rewrite 规则。
# Pages 会自动把 /admin/login 解析到 /admin/login.html；
# 若再写 rewrite，会与 .html 后缀规范化互相冲突，产生 308 重定向循环。

